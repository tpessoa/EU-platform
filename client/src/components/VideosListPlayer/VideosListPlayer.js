import React, { useState, useEffect } from "react";

import PlayVideo from "../PlayVideo";

import { VideoWrapper } from "./VideosListPlayer.elements";

const VideosListPlayer = () => {
  // const [video, setVideo] = useState(null);

  // const getVideoUrl = (c_id, v_id) => {
  //   var obj = CategoryData.find((element) => element.id == c_id);
  //   if (obj.videos) {
  //     return obj.videos.find((element) => element.includes(v_id));
  //   }
  //   return null;
  // };

  return (
    <>
      {/* <VideoWrapper>
        <PlayVideo video_url={} />
      </VideoWrapper> */}
    </>
  );
};

export default VideosListPlayer;
